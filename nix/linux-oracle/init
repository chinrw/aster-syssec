#!/bin/sh

/bin/busybox mount -t devtmpfs devtmpfs /dev >/dev/null 2>&1 || true
printf '%s\n' SYSSEC_GUEST_READY
/syssec-case
case_status=$?
/bin/busybox sync
/bin/busybox poweroff -f

printf 'syssec-case exited %s; poweroff failed\n' "$case_status" >&2
while :; do
    /bin/busybox sleep 3600
done
